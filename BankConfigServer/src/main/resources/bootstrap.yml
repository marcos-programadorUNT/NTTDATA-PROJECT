server:
    port: 1234
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/marcos-programadorUNT/NTTDATA-PROJECT.git
          search-paths: ClientConfiguration
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}
          default-label: emilio
  security:
    user:
      name: root
      password: s3cr3t